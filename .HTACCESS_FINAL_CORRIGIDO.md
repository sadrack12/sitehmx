# ✅ .htaccess Final - Considerando Backend Laravel

## 🔍 Problema Identificado

Você tem:
- Frontend em `public_html/` 
- Backend Laravel provavelmente em `public_html/api/`

O `.htaccess` do Laravel pode estar interferindo!

---

## ✅ Solução: .htaccess Principal

### No cPanel, edite `public_html/.htaccess`:

```apache
RewriteEngine On
RewriteBase /

# PRIORIDADE 1: Redirect específico para login (ANTES de tudo)
RewriteRule ^gestao/login/?$ /gestao/login.html [L,R=301]

# PRIORIDADE 2: Se arquivo ou diretório existe, servir diretamente
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# PRIORIDADE 3: Não tocar em requisições para API (deixa Laravel lidar)
RewriteCond %{REQUEST_URI} ^/api/
RewriteRule ^ - [L]

# PRIORIDADE 4: Não tocar em assets do Next.js
RewriteCond %{REQUEST_URI} ^/_next/
RewriteRule ^ - [L]

# PRIORIDADE 5: Tentar adicionar .html para outras rotas
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_URI} !^/api/
RewriteCond %{REQUEST_URI} !^/_next/
RewriteCond %{REQUEST_URI}\.html -f
RewriteRule ^(.*)$ $1.html [L]

# PRIORIDADE 6: Fallback para index.html (apenas se não encontrou nada)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]
```

---

## 🔑 Pontos Importantes

1. **Regra do login está PRIMEIRA** - processa antes de qualquer outra coisa
2. **API não é tocada** - deixa o Laravel lidar com `/api/`
3. **Assets não são tocados** - deixa Next.js servir `/_next/`
4. **Ordem é importante!** - processa de cima para baixo

---

## 📋 Checklist

- [ ] Atualizou `.htaccess` em `public_html/` com o conteúdo acima
- [ ] Verificou se não há `.htaccess` conflitante em outras pastas
- [ ] Limpou cache do navegador
- [ ] Testou `https://clamatec.com/gestao/login`

---

## 🚨 Se Ainda Não Funcionar

Me envie:
1. Conteúdo do `.htaccess` atual em `public_html/`
2. Se existe `.htaccess` em `public_html/api/`
3. Logs de erro do cPanel

Com essas informações, posso criar uma solução mais específica!

