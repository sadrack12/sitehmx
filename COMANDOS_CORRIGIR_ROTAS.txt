COMANDOS PARA CORRIGIR AS ROTAS:

# 1. Ir para o diretório do Laravel
cd ~/public_html/api

# 2. Editar AppServiceProvider
nano app/Providers/AppServiceProvider.php

# 3. Encontrar esta parte:
#    Route::prefix('api')->group(function () {
#        if (file_exists(base_path('routes/api.php'))) {
#            require base_path('routes/api.php');
#        }
#    });
#
# 4. Substituir por:
#    if (file_exists(base_path('routes/api.php'))) {
#        require base_path('routes/api.php');
#    }

# 5. Salvar: Ctrl+X, depois Y, depois Enter

# 6. Limpar cache
php artisan route:clear
php artisan config:clear

# 7. Recriar cache
php artisan route:cache
php artisan config:cache

# 8. Verificar rotas
php artisan route:list | grep public | head -5

# 9. Testar no navegador:
# https://clamatec.com/api/public/noticias

